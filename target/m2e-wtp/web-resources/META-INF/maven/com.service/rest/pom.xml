<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             http://maven.apache.org/xsd/maven-4.0.0.xsd">

	<!-- This element indicates what version of the object model this POM is 
		using. Declares to which version of project descriptor this POM conforms. 
		The version of the model itself changes very infrequently but it is mandatory 
		in order to ensure stability of use if and when the Maven developers deem 
		it necessary to change the model. -->
	<modelVersion>4.0.0</modelVersion>

	<!-- This element indicates the unique identifier of the organization or 
		group that created the project. The groupId is one of the key identifiers 
		of a project and is typically based on the fully qualified domain name of 
		your organization. -->
	<groupId>com.service</groupId>

	<!-- This element indicates the unique base name of the primary artifact 
		being generated by this project. The primary artifact for a project is typically 
		a JAR file. Secondary artifacts like source bundles also use the artifactId 
		as part of their final name. -->
	<artifactId>rest</artifactId>

	<!-- This element indicates the version of the artifact generated by the 
		project. Maven goes a long way to help you with version management and you 
		will often see the SNAPSHOT designator in a version, which indicates that 
		a project is in a state of development. -->
	<version>1.0.0-FINAL</version>

	<!-- This element indicates the package type to be used by this artifact 
		(e.g. JAR, WAR, EAR, etc.). This not only means if the artifact produced 
		is JAR, WAR, or EAR but can also indicate a specific lifecycle to use as 
		part of the build process. -->
	<packaging>war</packaging>

	<!-- This element indicates the display name used for the project. This 
		is often used in Maven's generated documentation. -->
	<name>com.service.rest</name>

	<!-- This element provides a basic description of your project. This is 
		often used in Maven's generated documentation. -->
	<description>web service project</description>

	<!-- ********************************************************** -->

	<!-- This element indicates where the project's site can be found. This 
		is often used in Maven's generated documentation. -->
	<url>${host.url.name}</url>

	<!-- The year of the project's inception, specified with 4 digits. This 
		value is used when generating copyright notices as well as being informational. -->
	<inceptionYear>2016</inceptionYear>

	<!-- This element describes various attributes of the organization to which 
		the project belongs. These attributes are utilized when documentation is 
		created (for copyright notices and links). -->
	<organization>
		<name>Default Inc.</name>
		<url>Default.com</url>
	</organization>

	<!-- Describes the committers of a project. -->
	<developers>
		<developer>
			<name>Brandon Howard</name>
			<id>bran</id>
			<email>brandon.howard0@email.com</email>
			<organization>Default Inc.</organization>
			<roles>
				<role>Technical Expert - developer</role>
			</roles>
		</developer>
	</developers>

	<!-- Describes the contributors to a project that are not yet committers. -->
	<contributors />

	<!-- Contains information about a project's mailing lists. -->
	<mailingLists>
		<mailingList>
			<name>DeveloperEmail</name>
			<post>brandon.howard0@email.com</post>
		</mailingList>
	</mailingLists>

	<!-- ********************************************************** -->

	<!-- The modules (sometimes called subprojects) to build as a part of this 
		project. Each module listed is a relative path to the directory containing 
		the module. To be consistent with the way default urls are calculated from 
		parent, it is recommended to have module names match artifact ids. -->
	<modules />

	<!-- Properties that can be used throughout the POM as a substitution, and 
		are used as filters in resources if enabled. -->
	<properties>
		<cdi-api.version>1.1</cdi-api.version>
		<gson.version>2.8.0</gson.version>
		<host.url.name>http://[hostname]:8080/rest/api</host.url.name>
		<java.version>1.8</java.version>
		<javaee.validation.api.version>1.1.0.Final</javaee.validation.api.version>
		<javaee.web.api.version>7.0</javaee.web.api.version>
		<junit.version>4.12</junit.version>
		<maven.compiler.plugin.version>3.1</maven.compiler.plugin.version>
		<maven.war.plugin.version>2.6</maven.war.plugin.version>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<slf4j.log4j12.version>1.7.21</slf4j.log4j12.version>	
	</properties>

	<!-- The lists of the remote repositories for discovering dependencies and 
		extensions. -->
	<repositories />

	<!-- This element describes all of the dependencies associated with a project. 
		These dependencies are used to construct a classpath for your project during 
		the build process. They are automatically downloaded from the repositories 
		defined in this project. -->
	<dependencies>
		<!-- JAVA WEB -->
		<dependency>
	    	<groupId>javax</groupId>
	    	<artifactId>javaee-web-api</artifactId>
	    	<version>${javaee.web.api.version}</version>
	    	<scope>provided</scope>
		</dependency>
		<!-- VALIDATION -->
		<dependency>
		    <groupId>javax.validation</groupId>
		    <artifactId>validation-api</artifactId>
		    <version>${javaee.validation.api.version}</version>
		    <scope>provided</scope>
		</dependency>
		<!-- TESTING -->
		<dependency>
		    <groupId>junit</groupId>
		    <artifactId>junit</artifactId>
		    <version>${junit.version}</version>
		</dependency>
		<!-- LOGGING -->
		<dependency>
			<groupId>org.slf4j</groupId>
			<artifactId>slf4j-log4j12</artifactId>
			<version>${slf4j.log4j12.version}</version>
		</dependency>
		<!-- MARSHALLING -->
		<dependency>
		    <groupId>com.google.code.gson</groupId>
		    <artifactId>gson</artifactId>
		    <version>${gson.version}</version>
		</dependency>
	</dependencies>

	<!-- The project's issue management system information. -->
	<issueManagement />

	<!-- The project's continuous integration information. -->
	<ciManagement />

	<!-- Distribution information for a project that enables deployment of the 
		site and artifacts to remote web servers and repositories respectively. -->
	<!-- distributionManagement>
		<repository>
			<id>repo id</id>
			<name>repo name</name>
			<url>http://</url>
		</repository>
		<snapshotRepository>
			<id>repo id</id>
			<name>repo name</name>
			<url>http://</url>
		</snapshotRepository>
	</distributionManagement -->

	<!-- Specification for the SCM used by the project, such as CVS, Subversion, 
		etc. -->
	<scm />

	<!-- Describes the prerequisites in the build environment for this project. -->
	<prerequisites />

	<!-- The lists of the remote repositories for discovering plugins for builds 
		and reports. -->
	<pluginRepositories />

	<!-- A listing of project-local build profiles which will modify the build 
		process when activated. -->
	<profiles />

	<!-- Information required to build the project. -->
	<build>
		<finalName>${project.artifactId}</finalName>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>${maven.compiler.plugin.version}</version>
				<configuration>
					<source>${java.version}</source>
					<target>${java.version}</target>
					<compilerArgument>-Xlint:all</compilerArgument>
					<showWarnings>true</showWarnings>
					<showDeprecation>true</showDeprecation>
				</configuration>
			</plugin>
			
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-war-plugin</artifactId>
				<version>${maven.war.plugin.version}</version>
				<configuration>
					<failOnMissingWebXml>false</failOnMissingWebXml>
				</configuration>
			</plugin>
		</plugins>
	</build>

	<!-- This element includes the specification of report plugins to use to 
		generate the reports on the Maven-generated site. These reports will be run 
		when a user executes mvn site. All of the reports will be included in the 
		navigation bar for browsing. -->
	<reporting />

</project>